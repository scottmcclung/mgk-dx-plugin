{
  "schema": {
    "export": {
      "description": "export sobject schema to various formats",
      "flags": {
        "format":"the format of the export (xls, csv, sql, or md)",
        "path":"the destination filepath",
        "sobject":"the named sobjects to be included in the export  - if ommitted, all custom and standard objects will be exported"
      }
    }
  }
}
