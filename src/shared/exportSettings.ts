import { TRANSLATED_FIELD_TYPES } from '../config/constants';
import { FIELD_HEADER_COLUMNS, SUMMARY_HEADER_COLUMNS } from '../config/columns';

// Re-export using the original names for backward compatibility
export const translatedFieldTypes = TRANSLATED_FIELD_TYPES;
export const headerMap = FIELD_HEADER_COLUMNS;
export const summaryHeaderMap = SUMMARY_HEADER_COLUMNS;
